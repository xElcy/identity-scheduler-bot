# 毎日更新される1週間の予定表

## 新メンバーを1人だけ追加

先に、予定表作成時に指定した対象ロールを新メンバーに付けてください。
管理者がDiscordで実行します。

```text
/schedule add-member member:@追加する人
```

指定カテゴリーに本人・管理者・BOT用の鍵部屋を作り、回答パネルを設置します。
その人の部屋やパネルがすでにある場合は再利用します。他の人の回答は変更しません。

## 毎日更新する

既存の予定表を切り替える、または受付終了した予定表を再開する場合：

```text
/schedule rolling
```

- 日本時間（TIMEZONE設定）の今日から7日間が対象です。9/15なら9/15〜9/21、翌日は9/16〜9/22です。
- 日付変更後、30秒以内を目安に更新を始めます。Discord側の混雑や通信障害時は遅れることがあります。
- すでに答えた日付・時間帯の回答はそのまま残り、新しく表示される日は未回答から始まります。
- 過ぎた日の回答も保存します。管理者の集計・決定候補は表示中の7日間を対象にします。
- 部屋とパネルを再利用します。昼・夜の2枠なら7日分を1画面に表示します。時間帯が多い場合はページ切り替えです。
- BOTの再起動時も現在の日付へ追いつきます。日程確定をしても自動更新と回答受付は続きます。
- `/schedule close` を明示的に実行すると受付と自動更新を停止します。再開は `/schedule rolling` です。

外部Googleカレンダー等への接続は不要です。PCのカレンダー画面ではなく、設定したタイムゾーンの日付に連動します。

新規作成では `dates` を省略すると自動更新になります。

```text
/schedule create title:クラン予定 blocks:昼ラン帯,夜ラン帯 target_role:@メンバー category:個人予定
```

固定日程が必要なときだけ `dates:2026-09-20,2026-09-21` のように指定します。
自動更新中に日付を手動で入れ直す必要はありません。

## 日程確定・全員への通知

日程告知先チャンネルで `/schedule set-announcement` を設定しておきます。
管理者部屋の決定候補ボタン、または次のコマンドで告知できます。

```text
/schedule decide date:2026-09-20 block:夜ラン帯
```

告知本文に `@everyone` を付けて送信します。BOTには告知先で「@everyone のメンション」権限が必要です。
同じ日時の連打・再実行は重複通知を防ぎます。送信結果が不明の場合も自動再送せず管理者に確認を促します。
各メンバーの通知設定によって、プッシュ通知が届かないことはあります。

## 履歴・編集

```text
/schedule history date:2026-09-14
/schedule edit title:クラン予定 blocks:昼ラン帯,夜ラン帯
```

履歴は現在（または最新）の予定表に保存された、その日の回答を管理者だけに表示します。
編集しても同じ日付・同じ時間帯名の回答は引き継ぎます。時間帯名を変更した場合は別の時間帯として未回答になりますが、元の回答は履歴に残ります。
`/schedule delete` は従来どおり、確認後に部屋と履歴を含む予定表を削除するコマンドです。

## 開発用チェック

```text
python -m unittest discover -s tests -v
```

日付更新、年越し、うるう日、停止後の追いつき、回答・部屋の保持、古いボタン、二重通知防止、権限チェックを検証します。

公式仕様：[定期更新](https://discordpy.readthedocs.io/en/latest/ext/tasks/index.html)・[Discordのメンション](https://docs.discord.com/developers/resources/message#allowed-mentions-object)。
